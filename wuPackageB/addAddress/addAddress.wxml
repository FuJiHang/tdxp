<!--pages/newAddress/newAddress.wxml-->
<!-- <text>pages/newAddress/newAddress.wxml</text> -->

<view class='box'>
  <!-- <view class='fromtop'>
    <view class='shibie'>
      <textarea class='shibietext' bindinput="userWhrite" data-copyPlate="{{copyPlate}}" value='{{copyPlate}}'
        disabled='{{false}}' placeholder='粘贴地址自动填写逗号分隔 收货人，手机号，地址'
        placeholder-style='font-size: 26rpx; color: #B1B1B1;'></textarea>
      <view class='shibiebtn' catchtap='clickshibie'>识别</view>
    </view>
  </view> -->
  <view class='fromeBox'>
    <view class='fromeone' wx:for="{{fromdata}}" wx:key="{{index}}">
      <view class='username'>{{item.title}}</view>
      <input data-index="{{index}}" wx:if='{{index !== 2}}' type='{{item.type}}' placeholder='{{item.pl}}'
        value='{{item.val}}' class='ipt' disabled='{{index == 2}}' bindinput="weachinput"></input>
      <text wx:if='{{index == 2}}' bindtap='bindRegionChange'
        class='{{!isSelected?"placeHolder":""}}'>{{region.province.name+' '+region.city.name+' '+region.area.name}}</text>
      <!-- <i class="right-arrow"></i> -->
      <view class='rightBtnBox' wx:if='{{index == 2}}'> </view>
    </view>
  </view>
  <view class='defluterAddressBox'>
    <view class='defaultetip'>设为默认</view>
    <!-- <switch checked="{{switch1Checked}}" bindchange="switch1Change" /> -->
    <van-checkbox value="{{ switch1Checked }}" bindtap="switch1Change"  checked-color="#50DD1A"></van-checkbox>
  </view>
  <!-- <view class='fjh'></view> -->

  <view class='submintBtn {{showSubmintBtn ? "showSubmintBtn" :""}} ' bindtap='clickSubmint'>保存地址</view>
</view>
<!-- <addr-picker show="{{showSelect}}" bindaddrevent="onSelectRegion"></addr-picker> -->
<litterfirshiaddress show="{{showSelect}}" bindaddrevent="onSelectRegion"></litterfirshiaddress>
<!-- litterfirshiaddress -->